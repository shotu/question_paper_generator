

exports.getQuestions = (marks, easy, medium, hard, callback) => {

  callback(null, {})
  // thirdPartyController.getRegreUsersData(pageNumber).then(data =>{
  //   let usersArray = data.data; 
  //   return  helper.convertJsonTocsv(usersArray);
  // }).then(csv_name => {
  //   callback(null, csv_name);
  // })
  // .catch(e=>{
  //   callback(e, null);
  // })
}